<%= form_for @ticket, :url => tickets_path, :html => { :multipart => true } do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.select :category, Ticket::CATEGORIES, :selected  => @ticket.category, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
 <div class="field">
    <%= f.text_field :phone %>
    <%= f.hidden_field :resolved , value: 0 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
